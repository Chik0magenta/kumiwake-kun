# Kumiwake-kun v0.1 — 組み分けくん 명세

공식 영문명: `Kumiwake-kun` · 일본어명: `組み分けくん` · GitHub repository: `kumiwake-kun`. 기존 `group-maker/` 배포 폴더는 유지한다.

## 목적과 범위

교수자가 약 30명의 학생 명단을 입력하고 결석자를 제외한 후, 무작위 조 편성 결과를 프로젝터로 보여주는 정적 웹 앱을 제공한다.

Vanilla HTML/CSS/JavaScript만 사용한다. 백엔드, 데이터베이스, 외부 의존성, npm, 빌드, 서버 통신, 분석·추적, localStorage/IndexedDB 등 영구 저장소를 사용하지 않는다. `index.html` 직접 열기와 정적 호스팅을 지원한다.

파일은 `index.html`, `style.css`, `app.js`, `README.md`, `SPEC.md`로 구성한다.

## 화면과 동작

1. **명단 입력**: textarea의 한 행을 한 학생으로 처리한다. 앞뒤 공백과 빈 행을 제거하되 동명이인을 제거하지 않는다. 비어 있으면 오류를 표시하고 진행하지 않는다.
2. **오늘의 출석**: 각 학생은 `{ id, name, present }` 객체이며 독립된 ID를 가진다. `crypto.randomUUID()`를 우선하고 미지원 환경에서는 증가하는 세션 ID를 사용한다. 기본값은 전원 출석이다. 개별 출석 토글, 전체 선택·해제, 출석/전체 인원 표시, 명단 수정 기능을 제공한다.
3. **조 편성 결과**: 출석 학생 배열의 복사본을 Fisher–Yates로 섞고 조 번호에 순환 배치한다. 원본 순서를 변경하지 않으며 각 조의 인원 차이는 최대 1명이다. 학생은 정확히 한 조에 배정된다. 결과는 큰 이름과 조 번호를 가진 카드로 표시한다. 일본어 UI에서는 `グループ 1`, 한국어 UI에서는 `1조`로 표시한다.

조 개수 기본값은 6이다. 1 이상의 정수만 허용하며 출석 학생이 없거나 조 개수가 출석 학생 수보다 많으면 화면에 오류를 표시한다.

**다시 섞기**는 현재 출석 상태와 마지막으로 편성한 조 개수를 유지한다. **출석 수정**은 상태를 유지하며 출석 화면으로 돌아간다. **명단 수정**은 textarea 원문을 유지한다. 명단 재적용은 새 학생 객체를 만들고 전원 출석으로 초기화한다. 이전 결과 회피는 범위 밖이므로 다시 섞어도 같은 결과가 나올 수 있다.

## UI와 접근성

데스크톱과 프로젝터를 우선한다. 실제 button과 label, 키보드 조작, 눈에 보이는 포커스, 오류 알림을 제공한다. 출석 버튼은 `aria-pressed`, 체크 기호, 출석/결석 문구를 사용해 색상만으로 상태를 표현하지 않는다. 화면 전환 시 제목으로 포커스를 이동한다. 이름은 HTML로 해석하지 않고 텍스트로 출력한다. 애니메이션은 사용하지 않는다.

### 결과 화면의 viewport 배치

- 실제 수업의 주요 편성은 6조와 8조이며 **30명 / 8조를 주요 레이아웃 검증 대상으로 취급한다.**
- 데스크톱 폭 1024 CSS px 이상에서는 6조는 3열 × 2행, 8조는 4열 × 2행으로 표시한다. 결과 DOM에 조 개수를 기록하여 편성과 재편성 때 적용한다.
- 30명의 이름과 모든 조 카드가 세로 스크롤 없이 한 viewport에 들어가야 한다. 대표 검증 viewport는 1280×720, 1366×768, 1920×1080이며, 작은 프로젝터용 1024×640도 확인한다. 크기는 화면 해상도가 아닌 브라우저 콘텐츠 영역의 CSS 픽셀, 확대율 100% 기준이다.
- 결과 화면에서만 상단 브랜드와 단계 안내를 숨긴다. 상단에 작은 언어 선택을 유지한다. 제목·출석 요약과 세 개의 버튼은 한 행에 배치하고 개인정보 안내의 여백을 줄인다. 재편성 상태 알림은 데스크톱에서 화면낭독기에 제공하되 시각적으로 숨겨 카드 공간을 확보한다. 카드 영역은 남은 viewport 높이를 사용한다. 입력·출석 화면의 레이아웃은 유지한다.
- 데스크톱 이름은 24–32px, 조 번호는 한국어 28px·일본어 26px로 유지한다. 9조는 3열 × 3행을 유지하되 이름 24px·조 번호 26px와 작은 카드 내부 여백을 사용한다. 스크롤을 없애기 위해 글자를 지나치게 줄이거나 내용을 숨기지 않는다.
- 이름은 한국어로 한정하지 않는다. 한글 표시 기준 약 6자 이름(예: `가나다라마바`, `야마모토유키`)이 한 줄에 자연스럽게 들어갈 카드 폭과 내부 여백을 확보한다. 일본어 이름도 그대로 표시한다. 이름 축약, 마스킹, 말줄임은 구현하지 않는다.
- 더 긴 이름이나 작은 viewport·확대 화면에서는 이름을 줄바꿈하고 필요한 스크롤을 허용한다. 고정 높이 잘림이나 overflow 숨김으로 이름을 가려서 완료 조건을 만족시키지 않는다. 폭 1024px 미만은 기존 반응형 배치를 따른다.

### 직접 레이아웃 검증 절차

1. 브라우저에서 `index.html`을 열고 개발자 도구의 반응형 화면에서 위 viewport를 지정한다. 일본어·한국어 각각 확인한다.
2. 아래 네 경우를 각각 명단 입력 → 출석 확인 → 조 편성 순서로 실행한다. 실제 개인정보 대신 가상 명단을 사용한다. 길이 검증에는 위 6자 예시를 30행 반복해도 된다. 동명이인은 독립된 학생으로 처리된다.

| 명단 | 조 개수 | 예상 배치 / 인원 |
| --- | --- | --- |
| 짧은 가상 이름 30명 | 6 | 3열 × 2행 / 각 5명 |
| 짧은 가상 이름 30명 | 8 | 4열 × 2행 / 4,4,4,4,4,4,3,3명 |
| 약 6자 가상 이름 30명 | 6 | 3열 × 2행 / 각 5명 |
| 약 6자 가상 이름 30명 | 8 | 4열 × 2행 / 4,4,4,4,4,4,3,3명 |

3. 네 경우 모두 화면 맨 위에서 마지막 카드·학생 이름·개인정보 안내까지 스크롤 없이 보이는지 확인한다. 이름이 한 줄로 표시되고 잘림·겹침·가로 스크롤이 없는지 확인한다.
4. 다시 섞기와 출석 수정 후 6조 ↔ 8조 변경 시에도 적절한 열 수와 모든 이름이 유지되는지 확인한다. 작은 화면에서는 스크롤하며 모든 이름과 버튼에 접근할 수 있어야 한다.

**완료 조건:** 특히 일반적인 데스크톱 viewport의 30명 / 8조 결과가 약 6자 이름을 포함해 세로 스크롤 없이 보이는 것을 실제 브라우저에서 확인해야 한다. CSS 수치 검토만으로 실제 렌더링 검증을 대체하지 않는다.

## 개인정보

입력 명단은 서버로 전송되거나 저장되지 않고 현재 브라우저에서만 처리된다는 안내를 표시한다. 앱 데이터는 페이지 메모리에만 존재한다. 외부 스크립트·CDN·폰트 및 추가 네트워크 요청을 사용하지 않는다.

## 검증과 완료 기준

README의 수동 검증 시나리오를 따른다. 빈 명단, 빈 행, 공백, 동명이인, 여러 결석자, 전원 결석, 잘못된 조 개수, 1명/1조를 처리해야 한다. 30명/6조는 `[5,5,5,5,5,5]`, 29명/6조는 `[5,5,5,5,5,4]`, 28명/6조는 `[5,5,5,5,4,4]`여야 한다. 재편성 및 출석·명단 수정 흐름이 작동하고 외부 통신·영구 저장·라이브러리 의존성이 없어야 한다.

로그인, 서버·DB·영구 저장, CSV/XLSX 가져오기, 내보내기, 편성 이력, 조건별 편성, PWA, QR, 학생 페이지, 여러 강의 관리, 이름 마스킹은 구현하지 않는다. TXT 읽기와 두 언어 지원만 v0.1의 추가 범위이다.


## v0.1 언어 지원

일본어(`ja`)가 최초 접속 및 새로고침 기본값이다. UI의 언어 선택으로 한국어(`ko`)와 즉시 전환하고 `<html lang>`도 갱신한다. 언어는 저장하지 않는다. 외부 i18n 의존성 없이 app.js의 번역 문자열 테이블을 사용한다. 정적 안내·버튼·placeholder·접근성 라벨·동적 인원·오류를 번역한다. 공식 프로젝트명은 항상 Kumiwake-kun / 組み分けくん을 유지하며 일본어 그룹 표현은 `グループ`를 사용한다.

언어 전환 시 textarea 원문, 학생 객체와 ID, 개별 출결, 숫자 input 값, 현재 그룹 배열과 학생 순서, 편성 횟수를 바꾸지 않는다. 표시를 다시 그릴 뿐 파싱·셔플·명단 초기화를 실행하지 않는다. 숨겨진 화면의 오류도 선택 언어로 갱신한다. 운영체제 파일 선택기와 브라우저 기본 파일 입력 문구의 언어는 브라우저 설정에 따른다.

## v0.1 TXT 입력

파일 선택 input과 File API의 `arrayBuffer()` 및 UTF-8 `TextDecoder`를 사용한다. `.txt`만 허용하며 UTF-8 BOM을 지원한다. 파일은 업로드하지 않는다. 파일 내용을 textarea에 채운 뒤 사용자가 확인하고 명단 적용을 누르면 기존 파서를 사용한다. 적용 전까지 현재 출결·결과는 바꾸지 않는다. 재적용 시 각 행에 새 ID를 부여하고 전원 출석으로 시작한다.

읽기 실패·잘못된 확장자·잘못된 UTF-8은 번역된 오류를 표시하며 이전 입력을 유지한다. 같은 파일을 다시 선택할 수 있다. 빈 파일은 읽을 수 있지만 빈 명단 적용은 거부한다. 범위를 단순하게 유지하기 위해 drag & drop, CSV, XLSX는 구현하지 않는다.

## v0.1 최종 테스트와 배포 기준

README의 테스트 표를 최종 검증 목록으로 사용한다. 30/6, 30/8, 29/6, 28/6, 1/1, 출결·재편성·잘못된 조 개수, UTF-8 TXT·Unicode·공백·동명이인, 두 언어 전환 중 상태 보존을 검증한다. 9그룹 무스크롤도 1280×720 이상에서 확인한다. 10그룹 이상은 무스크롤을 보장하지 않는다. 6·8그룹 결과와 약 6자 이름의 가독성을 우선하며 기능 범위를 더 확장하지 않는다.

사용자로부터 이전 버전의 이름 길이 및 9그룹 무스크롤 확인 결과를 전달받았다. 최종 일본어 UI와 언어 선택 추가 후의 실제 레이아웃 재검증은 별도로 필요하다. 현재 도구의 로컬 페이지 접근 차단을 우회하지 않는다. 코드 검증을 브라우저 검증으로 표시하지 않으며, 파일 선택기·무스크롤·Network/Storage 직접 검증이 남아 있다.

배포 전에 5개 앱·문서 파일에 실제 학생 개인정보가 없는지, 외부 API·추적·영구 저장이 없는지, project site의 상대 CSS/JS 경로가 맞는지 확인한다. GitHub의 `kumiwake-kun` 저장소, main 브랜치와 Pages 배포를 사용한다. 현 작업 환경에는 원격 저장소·gh CLI·저장소 생성 및 Pages 설정 도구가 없어 배포는 미실행이다. README에 정확한 수동 생성·push·Pages 활성화 절차를 제공한다. 최종 브라우저 테스트 통과 후 배포한다.
